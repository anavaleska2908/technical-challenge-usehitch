// import { api } from "~/utils/api";
// import TodoCard from "./TodoCard";
// import { TodoSchema } from "~/lib/schemas/TodoSchema";
// import { TodoCreateSchema } from "~/lib/schemas/TodoCreateSchema";


// export default function TodoList(getTodos: any) {


//   return (
//     <>
//       {getTodos.length ? getTodos.map((todo: TodoSchema) => {
//         return <TodoCard key={todo.id} id={todo.id} title={todo.title} image={todo.image ?? undefined} completed={todo.completed} />
//       }) : <p>Create your first to-do...</p>}
//     </>
//   )
// }
